#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
	char ch[] = "cooding";
	printf("%s\n", ch);
	printf("sizeof string = %d , length of the string = %d\n", sizeof(ch), strlen(ch));
}